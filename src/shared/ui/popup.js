/**
 * @file popup.js
 * @module shared.ui.popup
 *
 * Defines toolkit-independent popup layout contracts shared by Shell and
 * Preferences. The shared minimum keeps runtime sizing and persisted preferences
 * aligned.
 */

/** Minimum popup width preserving full-size transport controls with seek visible. */
export const POPUP_SEEK_CONTROLS_MIN_WIDTH = 350;
